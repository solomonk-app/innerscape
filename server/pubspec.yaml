name: innerscape_server
description: Backend proxy for Innerscape web deployment
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_static: ^1.1.2
  shelf_router: ^1.1.4
  http: ^1.1.2
